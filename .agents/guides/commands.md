# Useful Commands

Quick reference for build, test, run, and CLI commands. **No commands exist yet**
— the repo has no source or Makefile. Update this file in the same change that
adds each command.

> When commands land, document them here so agents do not guess. Run
> [`actions/verify-docs-in-sync.md`](../actions/verify-docs-in-sync.md) after
> changing flags or binaries.

## Build

```bash
# TBD — e.g. make all
```

## Test

```bash
# TBD — e.g. make test
```

## Run locally

### Server mode (planned)

```bash
# TBD — e.g. ./file-transfer server --root /path/to/files --listen :8080
```

### Client mode (planned)

```bash
# TBD — e.g. ./file-transfer client --host localhost:8080 --file report.pdf --output ./report.pdf
# TBD — e.g. ./file-transfer client --host localhost:8080 --file readme.txt   # stdout
```

## Clean

```bash
# TBD — e.g. make clean
```
